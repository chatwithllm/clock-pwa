.git
.gitignore
README.md
Dockerfile
docker-compose.yml
.dockerignore
.playwright-mcp
scratchpad
*.log
*.png.bak
# Dev-only — never served by nginx (Dockerfile COPYs files explicitly), so these
# just bloat the build context. Trim them out.
package.json
test
docs
.superpowers
.claude
graphify-out
node_modules
Caddyfile
